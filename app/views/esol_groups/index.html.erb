<h1>Listing esol_groups</h1>

<table>
  <tr>
    <th>Group name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @esol_groups.each do |esol_group| %>
  <tr>
    <td><%= esol_group.group_name %></td>
    <td><%= link_to 'Show', esol_group %></td>
    <td><%= link_to 'Edit', edit_esol_group_path(esol_group) %></td>
    <td><%= link_to 'Destroy', esol_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Esol group', new_esol_group_path %>
