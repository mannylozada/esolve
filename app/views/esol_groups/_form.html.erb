<%= form_for(@esol_group) do |f| %>
  <% if @esol_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@esol_group.errors.count, "error") %> prohibited this esol_group from being saved:</h2>

      <ul>
      <% @esol_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :group_name %><br />
    <%= f.text_field :group_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
